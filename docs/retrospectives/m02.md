# M02 Retrospective

_This file is drafted at plan start and filled in at M02 exit._

## What we shipped

- `sw/refbook/` C++17 library + pybind11 wrapper.
- Frozen headers: `TobDelta`, `BookEvent`, `synthetic_gen.py` schema.
- 7 PRs, 8 phases.
- ≥ 95% line coverage on `sw/refbook/src/` + `sw/refbook/include/`.
- 10 M-event synthetic reproducibility gate in CI.

## Performance (single core, developer host: _fill in CPU_)

| Benchmark | Median µs/event | Target | Status |
| --- | --- | --- | --- |
| `BM_StepAddOnly` | _fill in_ | ≤ 5 µs | |
| `BM_StepMixed` | _fill in_ | ≤ 5 µs | |

## What took longer than planned

_Fill in at exit._

## Which risks fired

_Cross-reference the risk table in the spec (§8)._

## Surprises

_Fill in at exit._

## Carried to M03

- Real-pcap validation of refbook (roadmap exit criterion #2) — blocked on
  NASDAQ TVITCH 5.0 subscription. `test_pcap_replay.cpp` is SKIPPED in CI;
  flip to active when pcaps arrive.
- `book_event_pkg.sv` in M03 must mirror `book_event.h` byte-for-byte; validate
  via CI diff check or codegen.
